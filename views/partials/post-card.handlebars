<div class="col col-12">
  <div class="card">
    <div class="card-header">
      <h2 class="card-title">{{title}}</h2>
      <h4 class="card-subtitle">{{createdAt}}</h4>
    </div>
    <div class="card-body">
      <p class="card-text">{{body}}</p>
    </div>
  </div>
</div>